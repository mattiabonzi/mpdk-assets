version: "2"
services:
  db:
    environment:
      POSTGRES_PASSWORD: "${MOODLE_DOCKER_DBPASS}"
      MYSQL_ROOT_PASSWORD: "${MOODLE_DOCKER_DBPASS}"
      MYSQL_PASSWORD: "${MOODLE_DOCKER_DBPASS}"
      SA_PASSWORD: "${MOODLE_DOCKER_DBPASS}"